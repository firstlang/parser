export default {
	codeIn: `Widget (
	// Rendering
	- Widgets render without mutating shared state.
	render() (
		- Compare output against (image:reference%20render.png).
		return true
	)
)`,
} satisfies ParseCase;
